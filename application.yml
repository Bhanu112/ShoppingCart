eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    hostname: localhost
    
spring:
  jpa:
      hibernate:
        ddl-auto: update
    datasource:
      url: jdbc:mysql://localhost:3306/shopii
      username: root
      password: root
